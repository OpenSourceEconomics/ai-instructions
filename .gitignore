# Claude Code
.claude/

# IDE
.idea/
.vscode/

# macOS
.DS_Store

# pixi -- no trailing slash: giving a scratch worktree a toolchain by symlinking
# another checkout's environment is common, and `.pixi/` matches a directory but
# not a symlink of that name, so `git add -A` commits a machine-local path.
.pixi

# Python
__pycache__/
*.py[cod]

# Ruff
.ruff_cache/
